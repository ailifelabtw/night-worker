node_modules/
*.log
.DS_Store
.env
.env.local

# 個人 config 不上 git（保留你自己的 brand voice / recent_hits 在本機）
config/personal*.json
config/runs/

# 本機跑的辯論結果不上 git（GH Actions cron 跑的會用 git add -f 強制推）
results/*.json


